# Walkthrough: Thai & US Stock Scanner Pro V5.1 (Precision Date & Live/Historical Sync)

เราได้แก้ไขตรรกะวันเวลา (Timezone Date Logic), โหมดสแกนสด vs สแกนย้อนหลัง, และการซิงค์กราฟแท่งเทียนให้ตรงกับวันสแกนแบบ 100% ตามข้อสังเกตของผู้ใช้เรียบร้อยแล้ว

---

## 🛠️ รายการปัญหาที่ตรวจพบและแนวทางแก้ไขใน V5.1

### 1. ปัญหา "หลงวัน คิดว่าวันที่ 10 เป็นวันที่ 11"
- **สาเหตุที่พบ**: 
  - ในฟังก์ชันคำนวณวันของ `index.html` มีการบวกชดเชยเวลาซ้ำซ้อน: `d.setMinutes(d.getMinutes() - d.getTimezoneOffset() + (7 * 60))`
  - บนเครื่องคอมพิวเตอร์ที่อยู่ในไทม์โซนประเทศไทย (`Asia/Bangkok`, UTC+7) ค่า `getTimezoneOffset()` ส่งกลับ `-420` นาที
  - เมื่อนำไปลบแล้วบวกเพิ่มอีก 420 นาที ทำให้ระบบบวกเวลาเบิ้ลไปถึง **+840 นาที (+14 ชั่วโมง)** ส่งผลให้ช่วงหัวค่ำของวันที่ 10 ก.ย. วันที่ถูกดีดข้ามไปเป็น **11 ก.ย.** ทันที!
- **การแก้ไข**:
  - เปลี่ยนมาใช้มาตรฐานสากล `Intl.DateTimeFormat` ที่รองรับ IANA Timezone โดยตรง:
    - 🇹🇭 ตลาดหุ้นไทย: อิงไทม์โซน `'Asia/Bangkok'`
    - 🇺🇸 ตลาดหุ้นสหรัฐฯ: อิงไทม์โซน `'America/New_York'`
  - วันที่ของวันนี้ (Today) จะส่งกลับ **`2026-09-10`** เสมอ ไม่กระโดดข้ามวันอีกต่อไป

---

### 2. ปัญหา "สแกนหุ้นอเมริกาแล้วไม่อัปเดตเป็นราคาปกติ"
- **สาเหตุที่พบ**:
  - เมื่อหน้าเว็บคิดว่าวันนี้คือวันที่ 11 ก.ย. พอผู้ใช้เลือกวันที่ 10 ก.ย. เพื่อสแกนตลาดสด หน้าเว็บกลับเข้าใจผิดว่าวันที่ 10 ก.ย. เป็น "วันในอดีต (Historical Backtest)"
  - ส่งผลให้ระบบเปิดแถบสีส้มโหมดย้อนรอยอดีต และไม่ยอมแสดงราคาเทรดสดของตลาดสหรัฐฯ ที่กำลังเปิดทำการอยู่
- **การแก้ไข**:
  - แยกโหมด **สแกนสด (Live Mode)** และ **สแกนย้อนหลัง (Historical Mode)** อย่างเด็ดขาด:
    - หากเลือก **วันนี้ (Today: 2026-09-10)**: ทำการดึงราคาซื้อขายสดๆ ณ วินาทีนั้น (Real-time Live Tick Feed) จาก TradingView Official Scanner ทันที พร้อมแสดงราคาแบบ Live (เช่น `AAPL: $319.67 (+1.37%)`, `MSFT: $491.62`)
    - หากเลือก **วันในอดีต (เช่น 2026-09-04)**: ทำการตัดแท่งเทียนประวัติศาสตร์ ณ สิ้นวันนั้น พร้อมแสดงราคาปิดของวันนั้นจริงๆ (Fact Close) และเปิดคอลัมน์เฉลยผลลัพธ์ (Outcome 5D & 20D)

---

### 3. ปัญหา "กราฟแท่งเทียนขึ้นไม่ตรงกับวันที่สแกน"
- **สาเหตุที่พบ**:
  - ไทม์สแตมป์แท่งเทียนของหุ้นสหรัฐฯ จากเซิร์ฟเวอร์ก่อนหน้านี้ถูกแปลงผ่านฟังก์ชัน Bangkok (+7 ชม.) ทำให้วันของแท่งเทียน US เหลื่อมล้ำกับวันทำการจริงในนิวยอร์ก
  - เมื่อกดดู Chart ตัวชี้เป้าสีเหลืองจึงเลื่อนไปชี้แท่งเทียนผิดแท่ง
- **การแก้ไข**:
  - เซิร์ฟเวอร์แปลงแท่งเทียนหุ้น US ด้วยไทม์โซน `'America/New_York'` และหุ้นไทยด้วย `'Asia/Bangkok'` 100%
  - เมื่อเปิดดู Chart:
    - **ในโหมดย้อนหลัง**: เส้นประสีเหลืองจะมาร์คลงบนแท่งเทียนของวันที่สแกนจริงเป๊ะๆ พร้อมข้อความ `🎯 วันที่สแกน: YYYY-MM-DD` และหัวข้อหน้าต่างระบุวันตรงกัน
    - **ในโหมดสด (วันนี้)**: แสดงแท่งเทียน 60 วันล่าสุดจนถึงวันนี้ พร้อมมาร์กเกอร์สีเขียว `⚡ ราคาตลาดล่าสุด (YYYY-MM-DD)` บนแท่งเทียนปัจจุบัน

---

## 🧪 ผลการทดสอบยืนยันระบบ (Automated Test Output)

รันสคริปต์ทดสอบครบทั้ง 7 จุด (`scratch/verify_all_fixes.js`):
1. **US Live Scan (2026-09-10)**: แสดงราคาซื้อขายสด `MSFT: $491.62`, `AAPL: $319.67`, `META: $652.15` (สถานะ `isHistorical: false`)
2. **Thai Live Scan (2026-09-10)**: แสดงราคาล่าสุดตรงรอบตลาด `AWC: ฿3.08`, `BGRIM: ฿19.70`, `CRC: ฿29.00`
3. **US Historical Scan (2026-09-04)**: แสดงราคาปิดของวันที่ 4 ก.ย. จริง `NVDA: $230.36`, `MSFT: $499.70` พร้อมผลตอบแทน 5 วันถัดไป
4. **Thai Historical Scan (2026-09-04)**: แสดงราคาปิดของวันที่ 4 ก.ย. จริง `PTT: ฿41.50`, `BGRIM: ฿19.70`
5. **Chart Modal Sync (Live & Historical)**: วันที่ในหัวข้อและเส้นมาร์คสีเหลืองบน Canvas ตรงกับวันทำการที่เลือก 100%

---

## 🚀 พร้อมใช้งานทันที
เปิดเว็บเบราว์เซอร์ใช้งานได้เลยที่: **[http://localhost:3300](http://localhost:3300)**
